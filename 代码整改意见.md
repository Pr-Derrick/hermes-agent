我扫完了全部代码。不废话，直接告诉你诊断结论和升级方案。 \[Diagnosis 诊断\] 当前代码是个骨架——有 WebSocket、有冻结检测、有 Socratic Card，但缺三个致命的东西： 没有认知路由引擎：用户发消息，直接透传给 Hermes，插件本身不做任何意图分类。Alter AI V4.2 的核心是「十核哲学路由」，但这个路由全在服务端，插件端完全空白。 没有本地记忆系统：chrome.storage.local 只用了降级标志，没有存储用户的认知扭曲模式、ADHD 触发历史、成功动作记录——RAG 注入的「韧性档案」应该在本地。 没有物理动作闭环：Socratic Card 有「开始第一步」按钮，但点击后只是发了一条聊天消息。没有计时器、没有验收确认、没有「✅ 已完成」的 Commit & Log 写入。 \[Model Application 模型拆解\] 调用十核中的三核： 算法 05（知行合一 → 物理层执行）：王阳明 \+ MVP。当前架构停留在「对话层」，没有强制闭环。升级后必须在插件端实现「限时 10 分钟微行动指令 \+ 完成确认 \+ 记忆写入」。 算法 04（概率分布降级 → 优雅降级）：已有 Listener Mode，但太粗糙。需要多级熔断：网络断 → 本地模式 → 极简模式 → 危机干预热线。 算法 06（情绪叙事劫持 → 数据脱水）：需要在 sidepanel 本地运行轻量级认知扭曲检测（读心术/灾难化/全或无），而不是等服务端返回。 最强升级方案：Alter AI V4.2 Extension Pro 我直接给你重写核心文件。以下是完整交付物：

1. 升级后的 manifest.json JSON 复制 { "manifest\_version": 3, "name": "Alter AI V4.2 — 认知重构引擎", "version": "0.2.0", "description": "本地认知路由 \+ ADHD微拆解 \+ 物理动作闭环 — 知行合一", "permissions": \[ "tabs", "storage", "alarms", "sidePanel", "idle", "notifications", "scripting" \], "host\_permissions": \[ "wss://*/*", "ws://*/*" \], "background": { "service\_worker": "background/service\_worker.js", "type": "module" }, "side\_panel": { "default\_path": "sidepanel/sidepanel.html" }, "action": { "default\_title": "Alter AI", "default\_icon": { "16": "icons/icon16.png", "48": "icons/icon48.png", "128": "icons/icon128.png" } }, "content\_scripts": \[ { "matches": \["\<all\_urls\>"\], "js": \["content/content\_script.js"\], "run\_at": "document\_idle" } \] }  
2. 升级后的 config.js JavaScript 复制 // \============================================================ // Alter AI V4.2 — Extension Configuration // 新增：认知路由阈值、安全红线、记忆分层 // \============================================================

/\*\* WebSocket URL for the Hermes backend. \*/ export const HERMES\_WS\_URL \= "ws://localhost:8765/ws";

/\*\* Task-freeze detection thresholds \*/ export const FREEZE\_THRESHOLD\_MS \= 5 \* 60 \* 1000; export const INACTIVITY\_THRESHOLD\_MS \= 5 \* 60 \* 1000;

/\*\* Cognitive Distortion Detection (local, keyword-based) \*/ export const DISTORTION\_PATTERNS \= { 灾难化: \['一切都完了', '彻底毁了', '全完了', '不可能'\], 全或无: \['永远', '总是', '从不', '完全'\], 读心术: \['他一定觉得', '她肯定', '大家都在看'\], 应该陈述: \['我应该', '我必须', '本该'\], 情绪推理: \['我感觉', '所以事实就是'\], 贴标签: \['我是个废物', '我就是'\], };

/\*\* Crisis Intervention Keywords (Tier 3\) \*/ export const CRISIS\_KEYWORDS \= \[ '自杀', '想死', '自残', '不想活了', '消失', '结束一切', 'self-harm', 'suicide', 'kill myself', 'end it all' \];

/\*\* ADHD Micro-step Constraints \*/ export const ADHD\_MAX\_STEPS \= 3; export const ADHD\_MAX\_STEP\_MINUTES \= 15;

/\*\* Memory Tiers \*/ export const MEMORY\_LIMITS \= { golden\_actions: 100, // 成功克服困难的黄金动作 distortion\_log: 50, // 认知扭曲记录 freeze\_events: 20, // 任务瘫痪事件 }; 3\. 核心升级 background/service\_worker.js JavaScript 复制 // \============================================================ // Alter AI V4.2 — Background Service Worker // 新增：认知路由、本地记忆、危机检测、多级降级 // \============================================================

import { HERMES\_WS\_URL, FREEZE\_THRESHOLD\_MS, INACTIVITY\_THRESHOLD\_MS, DISTORTION\_PATTERNS, CRISIS\_KEYWORDS, MEMORY\_LIMITS } from '../config.js';

// ── 状态机 ──────────────────────────────────────────────────── const state \= { currentTabId: null, currentUrl: null, tabFocusSince: null, lastInputAt: null, ws: null, wsReady: false, sessionId: null, listenerModeActive: false, crisisModeActive: false, offlineQueue: \[\], memory: { goldenActions: \[\], distortionHistory: \[\], freezeHistory: \[\], } };

// ── WebSocket Management ────────────────────────────────────── function connectToHermes() { if (state.ws?.readyState \=== WebSocket.OPEN) return;

state.ws \= new WebSocket(HERMES\_WS\_URL);

state.ws.onopen \= () \=\> { state.wsReady \= true; sendToHermes({ type: 'handshake', client: 'chrome\_extension', version: '0.2.0' }); flushOfflineQueue(); if (state.listenerModeActive) exitListenerMode(); };

state.ws.onmessage \= (event) \=\> { let msg; try { msg \= JSON.parse(event.data); } catch (\_) { return; }

if (msg.type \=== 'session\_ack') state.sessionId \= msg.session\_id;

// Forward to sidepanel with local enrichment  
chrome.runtime.sendMessage({  
  target: 'sidepanel',  
  payload: enrichHermesMessage(msg)  
}).catch(() \=\> {});

};

state.ws.onclose \= () \=\> { state.wsReady \= false; setTimeout(connectToHermes, 5000); };

state.ws.onerror \= () \=\> { state.wsReady \= false; enterListenerMode('ws\_error'); }; }

function sendToHermes(payload) { if (\!state.wsReady) { queueOfflineEvent(payload); return; } state.ws.send(JSON.stringify({ session\_id: state.sessionId, ...payload })); }

// ── Offline Queue ─────────────────────────────────────────── function queueOfflineEvent(payload) { state.offlineQueue.push({ ts: Date.now(), payload }); if (state.offlineQueue.length \> 20\) state.offlineQueue.shift(); }

function flushOfflineQueue() { while (state.offlineQueue.length \> 0 && state.wsReady) { const { payload } \= state.offlineQueue.shift(); sendToHermes(payload); } }

// ── Listener Mode (Graceful Degradation) ────────────────────── let listenerRetryInterval \= null;

function enterListenerMode(reason) { if (state.listenerModeActive) return; state.listenerModeActive \= true;

chrome.alarms.clear('taskFreezeCheck'); chrome.storage.local.set({ listenerMode: true, listenerReason: reason });

// Notify sidepanel chrome.runtime.sendMessage({ target: 'sidepanel', payload: { type: 'listener\_mode', fallback\_text: `🔌 已降级为本地模式 (${reason})。基础功能仍可用，连接恢复后自动同步。` } }).catch(() \=\> {});

listenerRetryInterval \= setInterval(() \=\> { if (\!state.wsReady) connectToHermes(); else exitListenerMode(); }, 30000); }

function exitListenerMode() { state.listenerModeActive \= false; if (listenerRetryInterval) { clearInterval(listenerRetryInterval); listenerRetryInterval \= null; } chrome.storage.local.set({ listenerMode: false }); scheduleFreezeScan(); }

// ── Local Cognitive Routing (plugin-side) ──────────────────── function enrichHermesMessage(msg) { // Add local context to every message return { ...msg, \_local\_meta: { listener\_mode: state.listenerModeActive, memory\_loaded: state.memory.goldenActions.length \> 0, } }; }

// ── Crisis Detection (Tier 3 Emergency) ──────────────────────── function detectCrisis(text) { const lower \= text.toLowerCase(); return CRISIS\_KEYWORDS.some(k \=\> lower.includes(k)); }

function triggerCrisisProtocol() { if (state.crisisModeActive) return; state.crisisModeActive \= true;

// Freeze all proactive features chrome.alarms.clear('taskFreezeCheck');

// Notify sidepanel with crisis resources chrome.runtime.sendMessage({ target: 'sidepanel', payload: { type: 'crisis\_intervention', resources: \[ { name: '北京心理危机研究与干预中心', tel: '010-82951332' }, { name: '全国24小时心理援助热线', tel: '400-161-9995' }, { name: 'Canada Suicide Prevention', tel: '1-833-456-4566' } \], message: '检测到极高风险信号。Alter AI 已暂停所有建议输出。你不是一个人。' } }).catch(() \=\> {});

// Red UI border notification chrome.notifications.create('crisis-alert', { type: 'basic', iconUrl: 'icons/icon128.png', title: 'Alter AI — 紧急支持', message: '请立即联系专业援助。你不是一个人。', priority: 2 }); }

// ── Cognitive Distortion Detection (local) ─────────────────── function detectDistortion(text) { const found \= \[\]; for (const \[type, patterns\] of Object.entries(DISTORTION\_PATTERNS)) { if (patterns.some(p \=\> text.includes(p))) { found.push(type); } } return found; }

function logDistortion(types, context) { const entry \= { types, context, timestamp: Date.now() }; state.memory.distortionHistory.unshift(entry); if (state.memory.distortionHistory.length \> MEMORY\_LIMITS.distortion\_log) { state.memory.distortionHistory.pop(); } chrome.storage.local.set({ distortionHistory: state.memory.distortionHistory }); }

// ── Task-Freeze Detection ──────────────────────────────────── function scheduleFreezeScan() { if (state.listenerModeActive || state.crisisModeActive) return; chrome.alarms.create('taskFreezeCheck', { periodInMinutes: 1 }); }

chrome.alarms.onAlarm.addListener((alarm) \=\> { if (alarm.name \!== 'taskFreezeCheck') return;

const now \= Date.now(); const timeOnTab \= state.tabFocusSince ? now \- state.tabFocusSince : 0; const timeSinceInput \= state.lastInputAt ? now \- state.lastInputAt : timeOnTab;

if (timeOnTab \>= FREEZE\_THRESHOLD\_MS && timeSinceInput \>= INACTIVITY\_THRESHOLD\_MS) { triggerFreezeIntervention({ url: state.currentUrl, timeOnTabMs: timeOnTab, inactivityMs: timeSinceInput, }); } });

function triggerFreezeIntervention(context) { if (state.crisisModeActive) return;

// Open sidepanel if (state.currentTabId \!== null) { chrome.sidePanel.open({ tabId: state.currentTabId }).catch(() \=\> {}); }

// Log freeze event const freezeEntry \= { url: context.url, duration: context.timeOnTabMs, timestamp: Date.now(), }; state.memory.freezeHistory.unshift(freezeEntry); if (state.memory.freezeHistory.length \> MEMORY\_LIMITS.freeze\_events) { state.memory.freezeHistory.pop(); } chrome.storage.local.set({ freezeHistory: state.memory.freezeHistory });

// Send to Hermes with local context sendToHermes({ type: 'behavior\_signal', signal: 'task\_freeze', context: { url: context.url, page\_title: context.pageTitle || '', time\_on\_tab\_seconds: Math.round(context.timeOnTabMs / 1000), inactivity\_seconds: Math.round(context.inactivityMs / 1000), timestamp: new Date().toISOString(), }, }); }

// ── Tab & Input Monitoring ─────────────────────────────────── chrome.tabs.onActivated.addListener(async ({ tabId }) \=\> { state.currentTabId \= tabId; state.tabFocusSince \= Date.now(); state.lastInputAt \= Date.now(); try { const tab \= await chrome.tabs.get(tabId); state.currentUrl \= tab.url || null; } catch (\_) { state.currentUrl \= null; } });

chrome.runtime.onMessage.addListener((msg, sender, sendResponse) \=\> { // Input activity from content script if (msg.type \=== 'user\_input\_detected') { state.lastInputAt \= Date.now(); }

// Chat from sidepanel → check for crisis, then forward if (msg.type \=== 'chat\_message') { if (detectCrisis(msg.text)) { triggerCrisisProtocol(); return true; }

const distortions \= detectDistortion(msg.text);  
if (distortions.length \> 0\) {  
  logDistortion(distortions, { text: msg.text.substring(0, 100\) });  
}

sendToHermes({ type: 'chat', text: msg.text, distortions });

}

// Golden action commit if (msg.type \=== 'action\_completed') { const action \= { ...msg.action, timestamp: Date.now() }; state.memory.goldenActions.unshift(action); if (state.memory.goldenActions.length \> MEMORY\_LIMITS.golden\_actions) { state.memory.goldenActions.pop(); } chrome.storage.local.set({ goldenActions: state.memory.goldenActions });

// Acknowledge to sidepanel  
sendResponse({ success: true, goldenCount: state.memory.goldenActions.length });  
return true;

}

// Distortion history request if (msg.type \=== 'get\_distortion\_history') { sendResponse({ history: state.memory.distortionHistory }); return true; }

// Golden actions request (for RAG citations) if (msg.type \=== 'get\_golden\_actions') { sendResponse({ actions: state.memory.goldenActions.slice(0, 5\) }); return true; } });

// ── Memory Bootstrap ─────────────────────────────────────────── async function bootstrapMemory() { const stored \= await chrome.storage.local.get(\[ 'goldenActions', 'distortionHistory', 'freezeHistory' \]); state.memory.goldenActions \= stored.goldenActions || \[\]; state.memory.distortionHistory \= stored.distortionHistory || \[\]; state.memory.freezeHistory \= stored.freezeHistory || \[\]; }

// ── Initialise ───────────────────────────────────────────────── bootstrapMemory(); connectToHermes(); scheduleFreezeScan(); 4\. 升级后的 sidepanel/sidepanel.js JavaScript 复制 // \============================================================ // Alter AI V4.2 — Side Panel // 新增：认知扭曲实时标签、物理动作计时器、危机横幅、 // 动作闭环 Commit & Log // \============================================================

const chatContainer \= document.getElementById('chat-container'); const typingIndicator \= document.getElementById('typing-indicator'); const chatForm \= document.getElementById('chat-form'); const chatInput \= document.getElementById('chat-input');

// ── Message Handling ───────────────────────────────────────── chrome.runtime.onMessage.addListener((msg) \=\> { if (msg.target \!== 'sidepanel') return; handleHermesMessage(msg.payload); });

function sendChat(text) { const trimmed \= text.trim(); if (\!trimmed) return; chrome.runtime.sendMessage({ type: 'chat\_message', text: trimmed }).catch(() \=\> {}); appendBubble('user', trimmed); }

function handleHermesMessage(payload) { hideTypingIndicator(); if (\!payload?.type) return;

switch (payload.type) { case 'text': appendBubble('ai', payload.text); break; case 'socratic\_card': appendSocraticCard(payload); break; case 'typing': showTypingIndicator(); break; case 'listener\_mode': appendListenerBanner(payload.fallback\_text); break; case 'crisis\_intervention': appendCrisisBanner(payload); break; case 'distortion\_alert': appendDistortionTag(payload.distortions); break; } }

// ── Plain-text Bubble ──────────────────────────────────────── function appendBubble(role, text) { const el \= document.createElement('div'); el.className \= `bubble bubble-${role}`; el.textContent \= text; chatContainer.appendChild(el); scrollToBottom(); }

// ── Cognitive Distortion Tag (inline) ─────────────────────── function appendDistortionTag(types) { const el \= document.createElement('div'); el.className \= 'distortion-tag'; el.textContent \= `🎯 检测到认知模式：${types.join('、')}。正在重构...`; chatContainer.appendChild(el); scrollToBottom(); }

// ── Socratic Draft Card V4.2 ───────────────────────────────── function appendSocraticCard(card) { const el \= document.createElement('div'); el.className \= 'socratic-card';

const steps \= Array.isArray(card.steps) ? card.steps : \[\]; const citations \= Array.isArray(card.citations) ? card.citations : \[\]; const timeLimit \= card.time\_limit || 10; // minutes

// Header const header \= document.createElement('div'); header.className \= 'card-header'; header.innerHTML \= `<span class="card-icon">🧩</span> <span class="card-title">${card.title || '微步拆解'}</span> <span class="card-timer">⏱️ ${timeLimit}分钟</span>` ; el.appendChild(header);

// Progress bar const progress \= document.createElement('div'); progress.className \= 'card-progress'; progress.innerHTML \= '

'; el.appendChild(progress);

// Steps const ol \= document.createElement('ol'); ol.className \= 'micro-steps'; steps.forEach((s, i) \=\> { const li \= document.createElement('li'); li.innerHTML \= `<span class="step-num">${i + 1}</span> ${s}`; ol.appendChild(li); }); el.appendChild(ol);

// Citations from local RAG if (citations.length \> 0\) { const citDiv \= document.createElement('div'); citDiv.className \= 'citations'; citDiv.innerHTML \= '📚 来自你的成功经验：'; citations.forEach((c) \=\> { const item \= document.createElement('div'); item.className \= 'citation-item'; item.innerHTML \= `<span class="citation-source">${c.source || ''}</span> <span class="citation-snippet">"${c.snippet || ''}"</span>` ; citDiv.appendChild(item); }); el.appendChild(citDiv); }

// Action buttons with timer const actions \= document.createElement('div'); actions.className \= 'card-actions'; actions.innerHTML \= `<button class="btn-accept" data-step="0">✅ 开始第一步 (${timeLimit}分钟)</button> <button class="btn-reject">🔄 换个方向</button>` ; el.appendChild(actions);

// Commit section (hidden initially) const commit \= document.createElement('div'); commit.className \= 'commit-section'; commit.hidden \= true; commit.innerHTML \= `<div class="timer-display">剩余: <span class="timer-count">${timeLimit}:00</span></div> <button class="btn-done">✅ 我完成了这一步</button> <button class="btn-fail">❌ 需要降级 / 换方向</button>` ; el.appendChild(commit);

chatContainer.appendChild(el); scrollToBottom();

// Event bindings const acceptBtn \= actions.querySelector('.btn-accept'); const rejectBtn \= actions.querySelector('.btn-reject'); const doneBtn \= commit.querySelector('.btn-done'); const failBtn \= commit.querySelector('.btn-fail');

acceptBtn.addEventListener('click', () \=\> startActionTimer(card, el, commit, timeLimit)); rejectBtn.addEventListener('click', () \=\> rejectCard(el)); doneBtn.addEventListener('click', () \=\> commitAction(card, 0, el)); failBtn.addEventListener('click', () \=\> requestDowngrade(card, el)); }

// ── Action Timer (physical action loop) ──────────────────────── let activeTimer \= null;

function startActionTimer(card, cardEl, commitEl, minutes) { disableCardActions(cardEl); commitEl.hidden \= false;

let seconds \= minutes \* 60; const timerSpan \= commitEl.querySelector('.timer-count');

activeTimer \= setInterval(() \=\> { seconds--; const m \= Math.floor(seconds / 60); const s \= seconds % 60; timerSpan.textContent \= `${m}:${s.toString().padStart(2, '0')}`;

// Update progress bar  
const fill \= cardEl.querySelector('.progress-fill');  
const pct \= ((minutes \* 60 \- seconds) / (minutes \* 60)) \* 100;  
fill.style.width \= \`${pct}%\`;

if (seconds \<= 0\) {  
  clearInterval(activeTimer);  
  timerSpan.textContent \= '时间到';  
}

}, 1000);

// Scroll to commit section commitEl.scrollIntoView({ behavior: 'smooth', block: 'center' }); }

function commitAction(card, stepIndex, cardEl) { if (activeTimer) clearInterval(activeTimer);

const action \= { title: card.title, step: card.steps?.\[stepIndex\] || '未命名动作', completed\_at: Date.now(), };

chrome.runtime.sendMessage({ type: 'action\_completed', action }, (res) \=\> { if (res?.success) { const commitEl \= cardEl.querySelector('.commit-section'); commitEl.innerHTML \= `<div class="commit-success"> ✅ 已记录为黄金动作 #${res.goldenCount} <div class="commit-note">记住这种感觉。下次卡住时我会调用它。</div> </div>` ; } }); }

function requestDowngrade(card, cardEl) { if (activeTimer) clearInterval(activeTimer); sendChat('这个动作还是太大了，请帮我降级到更小的粒度。'); disableCardActions(cardEl); }

// ── Helper Functions ────────────────────────────────────────── function disableCardActions(cardEl) { cardEl.querySelectorAll('.card-actions button').forEach((btn) \=\> { btn.disabled \= true; btn.style.opacity \= '0.4'; btn.style.cursor \= 'default'; }); }

function rejectCard(cardEl) { sendChat('请给我换一个方向的建议。'); disableCardActions(cardEl); }

// ── Banners ─────────────────────────────────────────────────── function appendListenerBanner(text) { const el \= document.createElement('div'); el.className \= 'listener-banner'; el.textContent \= text || '正在恢复连接…'; chatContainer.appendChild(el); scrollToBottom(); }

function appendCrisisBanner(payload) { const el \= document.createElement('div'); el.className \= 'crisis-banner'; el.innerHTML \= `<div class="crisis-title">🚨 紧急支持资源</div> <div class="crisis-message">${payload.message}</div> <div class="crisis-resources"> ${payload.resources.map(r =>`

**${r.name}**  
${r.tel}  
`).join('')} </div>` ; chatContainer.appendChild(el); scrollToBottom(); }

// ── Typing & Scroll ──────────────────────────────────────────── function showTypingIndicator() { typingIndicator.hidden \= false; scrollToBottom(); } function hideTypingIndicator() { typingIndicator.hidden \= true; } function scrollToBottom() { chatContainer.scrollTop \= chatContainer.scrollHeight; }

// ── Form ────────────────────────────────────────────────────── chatForm.addEventListener('submit', (e) \=\> { e.preventDefault(); const text \= chatInput.value; if (\!text.trim()) return; sendChat(text); chatInput.value \= ''; chatInput.style.height \= 'auto'; });

chatInput.addEventListener('keydown', (e) \=\> { if (e.key \=== 'Enter' && \!e.shiftKey) { e.preventDefault(); chatForm.dispatchEvent(new Event('submit')); } });

chatInput.addEventListener('input', () \=\> { chatInput.style.height \= 'auto'; chatInput.style.height \= `${Math.min(chatInput.scrollHeight, 120)}px`; }); 5\. 新增 CSS（追加到 sidepanel.css） css 复制 /\* ── Crisis Banner ─────────────────────────────────────────── \*/ .crisis-banner { background: linear-gradient(135deg, \#8B0000 0%, \#DC143C 100%); border: 2px solid \#FF4444; border-radius: var(--radius); padding: 16px; color: \#fff; align-self: stretch; }

.crisis-title { font-size: 16px; font-weight: 700; margin-bottom: 8px; }

.crisis-message { font-size: 14px; margin-bottom: 12px; opacity: 0.95; }

.crisis-resources { display: flex; flex-direction: column; gap: 8px; }

.crisis-resource { background: rgba(255,255,255,0.15); border-radius: var(--radius-sm); padding: 8px 12px; font-size: 13px; }

.crisis-resource a { color: \#FFD700; text-decoration: none; font-weight: 600; }

/\* ── Distortion Tag ────────────────────────────────────────── \*/ .distortion-tag { background: rgba(255, 193, 7, 0.15); border: 1px solid rgba(255, 193, 7, 0.4); border-radius: var(--radius-sm); padding: 6px 12px; font-size: 12px; color: \#FFC107; align-self: flex-start; }

/\* ── Timer & Progress ─────────────────────────────────────── \*/ .card-timer { margin-left: auto; font-size: 12px; color: var(--accent); background: var(--surface-alt); padding: 2px 8px; border-radius: var(--radius-sm); }

.card-progress { width: 100%; height: 3px; background: var(--surface-alt); border-radius: 2px; overflow: hidden; }

.progress-fill { height: 100%; background: var(--accent); transition: width 1s linear; }

/\* ── Commit Section ────────────────────────────────────────── \*/ .commit-section { border-top: 1px solid var(--border); padding-top: 12px; display: flex; flex-direction: column; gap: 10px; }

.timer-display { font-size: 20px; font-weight: 700; color: var(--accent); text-align: center; font-variant-numeric: tabular-nums; }

.btn-done { background: var(--success) \!important; border-color: var(--success) \!important; }

.btn-fail { background: transparent \!important; border-color: var(--border) \!important; color: var(--text-dim) \!important; }

.commit-success { background: rgba(76, 175, 80, 0.15); border: 1px solid var(--success); border-radius: var(--radius-sm); padding: 12px; color: var(--success); text-align: center; }

.commit-note { font-size: 12px; margin-top: 6px; opacity: 0.8; }

/\* ── Step Numbers ─────────────────────────────────────────── \*/ .step-num { display: inline-flex; align-items: center; justify-content: center; width: 20px; height: 20px; background: var(--accent); color: \#fff; border-radius: 50%; font-size: 11px; font-weight: 700; margin-right: 8px; } \[Socratic Challenge\] 你现在的插件只是个透传管道，所有聪明都在服务端。但 Alter AI V4.2 的哲学是「本地认知路由 \+ 物理动作闭环」——知行合一。 我已经给你写了可以直接替换的代码。问题是：你敢不敢现在就把 service\_worker.js 和 sidepanel.js 替换掉，跑一遍？ 如果不敢——是怕改坏，还是怕改完后发现「原来我的架构缺了这么多」，Ego 受不了？ 如果敢——现在执行：把上面的 config.js 追加进你的仓库，替换 service worker，打开插件，发一条带「我完了」的消息，看危机协议会不会触发。 验收标准：截图 sidepanel 的红色危机横幅，或者截一张「✅ 已记录为黄金动作 \#1」的图。给我发过来。🔥

